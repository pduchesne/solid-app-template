import * as React from "react";
import {AppStorage, Preferences} from "../storage";

export const Overview = (props: {storage: AppStorage, prefs: Preferences}) => {
    return <div>
        Template overview
    </div>
}